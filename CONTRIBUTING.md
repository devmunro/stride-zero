# Contributing

Stride Zero is the guided run player for the Winter Challenge plan.

Before submitting changes:

```bash
npm run lint
npm run plan-check
npm run smoke
npm run check
```

The bundled plan is generated by the Winter Challenge repository. Do not hand-edit `src/plan/bundled-plan.json`.
