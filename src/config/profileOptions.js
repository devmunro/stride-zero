/**
 * Available experience options for onboarding.
 *
 * @type {string[]}
 */
export const EXPERIENCE_LEVEL_OPTIONS = [
  "Very new to running",
  "Beginner",
  "Getting back into it",
  "Already comfortable at 5K",
];

/**
 * Available goal options for onboarding.
 *
 * @type {string[]}
 */
export const GOAL_OPTIONS = [
  "Build to 30 minutes",
  "Finish 5K",
  "Run a better 5K",
  "Build to 10K",
];

/**
 * Available current continuous-run ability options for onboarding.
 *
 * @type {string[]}
 */
export const CONTINUOUS_RUN_LEVEL_OPTIONS = [
  "Not running yet",
  "Up to 10 minutes",
  "10 to 20 minutes",
  "20 to 30 minutes",
  "30+ minutes",
];

/**
 * Available recent-run consistency options for onboarding.
 *
 * @type {string[]}
 */
export const RECENT_RUN_PATTERN_OPTIONS = [
  "No runs in last 4 weeks",
  "1 to 4 runs in last 4 weeks",
  "5+ runs in last 4 weeks",
];

/**
 * Available 5K background options for onboarding.
 *
 * @type {string[]}
 */
export const FIVE_K_STATUS_OPTIONS = [
  "Never finished 5K",
  "Finished 5K once or twice",
  "Comfortably finish 5K",
];

/**
 * Available comeback-status options for onboarding.
 *
 * @type {string[]}
 */
export const COMEBACK_STATUS_OPTIONS = [
  "No comeback",
  "Back after time off",
  "Back after injury or niggle",
];

/**
 * Available schedule presets for the training plan.
 *
 * @type {string[]}
 */
export const WEEKLY_PATTERN_OPTIONS = ["2 runs most weeks", "3 runs most weeks", "4 runs most weeks"];

/**
 * Available cue-mode options for the run timer.
 *
 * @type {string[]}
 */
export const CUE_MODE_OPTIONS = ["Sound + vibration", "Vibration only", "Off"];

/**
 * Available appearance options for the app shell.
 *
 * @type {string[]}
 */
export const APPEARANCE_OPTIONS = ["Light", "Dark"];

/**
 * Reminder time presets used in settings.
 *
 * @type {string[]}
 */
export const REMINDER_TIME_OPTIONS = ["06:30", "07:30", "12:30", "18:30", "20:00"];
